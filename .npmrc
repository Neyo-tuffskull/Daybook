# Hoisting off by default: a package that imports something it did not declare
# should fail here rather than in production.
node-linker=isolated
strict-peer-dependencies=false
auto-install-peers=true
